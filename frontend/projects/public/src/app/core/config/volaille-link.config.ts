export interface VollaileLinkPublicConfig {
  /**
   * Numéro WhatsApp professionnel de Vollaile Link.
   * Laisser vide jusqu'à la fourniture du numéro officiel.
   */
  whatsappNumber: string;
}

export const VOLLAILE_LINK_PUBLIC_CONFIG: VollaileLinkPublicConfig = {
  whatsappNumber: ''
};
